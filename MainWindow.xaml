<Window x:Class="CustomGadgetApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Custom Gadget" Height="200" Width="400"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent">
    <Grid Background="#AAFFFFFF">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBlock x:Name="ClockTextBlock" Text="08:32" FontSize="32" VerticalAlignment="Center" HorizontalAlignment="Center" />

        <Button Content="⚙" Width="30" Height="30" Grid.Column="1" Margin="10"
                HorizontalAlignment="Right" VerticalAlignment="Top"
                Click="SettingsButton_Click" ToolTip="Settings"/>
    </Grid>
</Window>
