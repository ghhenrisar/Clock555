<Window x:Class="Clock555.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Clock555"
        mc:Ignorable="d"
        Title="Clock555"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Window.Resources>
        <Style TargetType="Button" x:Key="WindowControlButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="20"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#555"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border CornerRadius="10" Background="#222">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>       <RowDefinition Height="Auto"/>       </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#333" CornerRadius="10,10,0,0" MouseLeftButtonDown="Window_MouseLeftButtonDown" Padding="1">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="-" Click="Minimize_Click" Style="{StaticResource WindowControlButton}"/>
                    <Button Content="⚙" Click="OpenSettings_Click" Style="{StaticResource WindowControlButton}"/>
                    <Button Content="⛶" Click="ToggleFullScreen_Click" Style="{StaticResource WindowControlButton}"/>
                    <Button Content="X" Click="Close_Click" Style="{StaticResource WindowControlButton}"/>
                </StackPanel>
            </Border>

            <TextBlock x:Name="ClockText"
                       Grid.Row="1"
                       Text="22:22"
                       FontFamily="Segoe UI Light"
                       Foreground="White"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Padding="0,0,0,0" /> <Calendar x:Name="CalendarControl" Visibility="Collapsed" Grid.Row="1"/>

        </Grid>
    </Border>
</Window>