<Window x:Class="CustomGadgetApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CustomGadgetApp"
        mc:Ignorable="d"
        Title="Custom Gadget" 
        SizeToContent="WidthAndHeight"   MinWidth="100" MinHeight="100"  Background="Transparent" AllowsTransparency="True" WindowStyle="None"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        Topmost="True">

    <Border CornerRadius="15" BorderBrush="Gray" BorderThickness="1">
        <Grid>
            <Grid.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#222" Offset="0.0"/>
                    <GradientStop Color="#333" Offset="1.0"/>
                </LinearGradientBrush>
            </Grid.Background>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#303030" Height="30" MouseLeftButtonDown="Window_MouseLeftButtonDown">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="-" Width="30" Height="30" Background="Transparent" Foreground="White" BorderBrush="Transparent" Click="Minimize_Click"/>
                    <Button Content="⚙️" Width="30" Height="30" Background="Transparent" Foreground="White" BorderBrush="Transparent" Click="OpenSettings_Click"/>
                    <Button Content="⛶" Width="30" Height="30" Background="Transparent" Foreground="White" BorderBrush="Transparent" Click="ToggleFullScreen_Click"/>
                    <Button Content="X" Width="30" Height="30" Background="Transparent" Foreground="White" BorderBrush="Transparent" Click="Close_Click"/>
                </StackPanel>
            </Border>

            <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock x:Name="ClockText"
                           FontSize="24"
                           Foreground="White"
                           HorizontalAlignment="Center"
                           Margin="0,5"/>

                <Calendar x:Name="CalendarControl"
                          Margin="0,20,0,0"
                          Visibility="Collapsed"
                          Background="#202020"
                          BorderBrush="Transparent"
                          Foreground="White"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>